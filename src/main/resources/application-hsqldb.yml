# ===================================================================
# Profile for MySql storage
# ===================================================================
spring:
  # DATASOURCE
  datasource:
    url: jdbc:hsqldb:file:~/trio-hsql # ;shutdown=true;hsqldb.write_delay=false
    username: SA
    password:
    driver-class-name: org.hsqldb.jdbc.JDBCDriver
flyway:
  placeholders: {"BIGINT_AUTO_INCREMENT": "BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1)"}
